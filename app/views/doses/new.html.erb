<h1>Add a new dose to <%= cocktail.name %></h1>

<%= simple_form_for (@cocktail, @dose) do |f| %>
  <%= f.association :ingredients %>
  <%= f.input :doses %>
  <%= f.button :submit %>
<% end %>

<p><%= link_to "Back to all cocktails", cocktails_path %></p>
